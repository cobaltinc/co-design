import { createStyles } from '@co-design/styles';

export default createStyles((theme) => {
  return {
    root: {
      position: 'relative',
      display: 'inline-block',
      cursor: 'pointer',
    },
    input: {
      display: 'none',
    },
  };
});
