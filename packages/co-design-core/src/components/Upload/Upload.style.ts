import { createStyles } from '@co-design/styles';

export default createStyles(() => {
  return {
    root: {
      position: 'relative',
      display: 'inline-block',
      cursor: 'pointer',
    },
    input: {
      display: 'none',
    },
  };
});
