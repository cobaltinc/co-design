import React from 'react';
import { Portal } from '../Portal';

export default {
  title: '@co-design/primitive/Portal',
  component: Portal,
};

export const Default = () => {
  return (
    <div>
      <Portal>Portal</Portal>
    </div>
  );
};
